<!DOCTYPE html>
<html>
  <head>
    <title>SearchAnalytics</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <main>
      <div class="list">
        <% if user_signed_in? %>
          <nav>
            <div>Welcome <%= current_user.name %></div>
            <%= button_to "Sign out", destroy_user_session_path, method: :delete, data: { turbo: false }, class:"btn_signout_from btn_hover" %>
          </nav>
        <% end %>

        <div class="title">
          <h1>Helpjuice Search Analytics</h1>
          <p>
            Welcome to the search analytics app. This app will help you find out what your users are searching for on your Helpjuice knowledge base.
          </p>
        </div>
        
        <div>
          <p><%= notice %></p>
          <p><%= alert %></p>
        </div>
        <%= yield %>
      </div>
    </main>
  </body>
</html>
